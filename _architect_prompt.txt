你是一个架构师Agent。请先读取 D:/projectsme/licang/ARCHITECT_CONTEXT.md 了解项目全貌，然后读取 D:/projectsme/licang/docs/ 下的所有设计文档。你的任务是：

1. 分析完整的项目需求和设计文档
2. 将P0 MVP拆解为单一切入点级别的编码任务（每个任务2-5分钟可完成）
3. 为每个任务定义：目标、需要创建/修改的文件（精确路径）、输入、输出、验收标准
4. 将完整的任务计划写入 D:/projectsme/licang/TASK_PLAN_P0.md

任务拆解要求：
- 粒度：每个任务只做一件事（如创建User实体类、编写注册接口、建user表SQL）
- 顺序：按依赖关系排序（先数据库→再实体→再服务→再控制器）
- 格式：每个任务包含 Objective / Files / Steps / Verification
- 覆盖：建表SQL、后端Java代码、API接口，全部覆盖P0 MVP范围
- 项目路径：D:/projectsme/licang/backend/

P0 MVP范围：
1. 用户系统：手机号注册/登录、JWT认证
2. 收藏CRUD：创建、查看、编辑、删除收藏
3. 自动解析：粘贴链接自动识别资源类型，自动填充标题、来源、封面
4. 分类管理：自定义一级/二级分类文件夹
5. 标签系统：系统自动标签 + 用户自定义标签
6. 多端同步：云端实时同步
7. 内置预览：视频播放器、图文阅读、MD渲染（后端只做数据支持）
8. 搜索筛选：全文搜索 + 类型/标签/分类筛选

请直接开始，不要询问确认。输出任务计划到指定文件。
